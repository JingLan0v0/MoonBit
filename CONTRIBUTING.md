# 参与维护

先阅读 README、`docs/architecture.md` 和 `docs/limitations.md`，安装 `toolchain.json` 固定的工具链。

## 修改与验证

1. 从 main 建立工作分支，描述要解决的实际问题和预期行为。
2. 核心实现放在 `src/`，对应包的 MoonBit 测试留在同目录；CLI 进程测试放在 `tests/`。
3. 行为变更同时更新约定和有针对性的测试。新增例子使用虚构数据。
4. 运行 `moon info --target js`、`moon fmt` 和 `node scripts/verify.mjs`，检查公共 `.mbti` 接口是否发生预期变化。
5. 提交可独立审查的有效改动，在 PR 中说明问题、修改及实际验证结果。不要制造空提交或为了次数拆分改动。

## 兼容性

- 导入路径为 `JingLan0v0/moonrow`，物理源码在 `src/`。
- JSON 报告通过 `schema_version` 标记结构版本；更改字段含义或删除字段须评估兼容性。
- CLI 的 0/1/2 退出码是公开约定，1 表示发现差异。
- 当前支持 JS 后端；未经验证不宣称其他后端可用。
- 不把编译产物、凭据、报名证件或真实客户数据提交到仓库。

## 缺陷报告

使用 GitHub 的问题模板，附上最小虚构输入、完整命令、预期/实际输出和工具版本。
测试通过不代表不存在缺陷；不确定时先提供可复现证据。
